#include <prime_breakdown_core.h>

#include <iostream>

std::map<std::string, double> compute_total(std::vector<order>* orders) {
    std::map<std::string, double> shipping_total;

    return shipping_total;
}

void test_function() {
    std::cout << "WOW" << std::endl;
}